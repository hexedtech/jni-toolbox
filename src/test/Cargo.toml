[package]
name = "jni-toolbox-test"
description = "test binary for jni-toolbox"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "test.rs"

[dependencies]
jni-toolbox-macro = { path = "../../macro/" }
jni-toolbox = { path = "../.." }
jni = "0.22"
thiserror = "2.0"
